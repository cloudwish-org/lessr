# lessr-cli

The lessr binary. Exposes run(PipelineBuilder) so the Pro binary registers more stages and calls the same entry point. Commands: init, hook <agent>, serve, gain, show <id>, bench, on/off <stage>, uninstall, pro.

See ../../docs/ARCHITECTURE.md and ../../docs/MECHANISMS.md for the contracts this crate implements.
