# lessr-dedup

Stage dedup, read-like tools only. Keys on absolute path; stores blake3 and turn. Unchanged file → one line; changed file → unified diff plus a handle to the full content. Counting: full bytes − reply bytes, exact.

See ../../docs/ARCHITECTURE.md and ../../docs/MECHANISMS.md for the contracts this crate implements.
